rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Server-side admin SDK bypasses these rules. Keep default deny for direct client access.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
